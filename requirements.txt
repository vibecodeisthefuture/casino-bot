# casino-bot runtime dependencies (regenerated from actual imports, 2026-08-28)
# All resolve to prebuilt cp312 manylinux wheels — no compiler needed in the slim image.

# Discord bot framework (2.x API: intents, setup_hook, discord.ui, async cogs)
discord.py>=2.4,<3

# Web backend that hosts the demo API and launches the bot in-process
fastapi>=0.115,<1
uvicorn[standard]>=0.30

# Configuration + validation (Pydantic v2) and .env loading
pydantic>=2.7,<3
python-dotenv>=1.0

# HTTP client used by handlers/slots (also a discord.py dependency)
aiohttp>=3.10,<4

# Image rendering for card tables and slot reels
Pillow>=10.4
